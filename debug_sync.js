const { Client } = require('pg'); const client = new Client({ connectionString: 'postgresql://postgres:0000@localhost:5432/securemail?schema=public' }); async function run() { await client.connect(); const res = await client.query('SELECT * FROM "MailBox" ORDER BY id DESC LIMIT 1'); console.log('Mailbox:', res.rows[0]); const logs = await client.query('SELECT * FROM "SyncLog" ORDER BY id DESC LIMIT 3'); console.log('Logs:', logs.rows); await client.end(); } run();
